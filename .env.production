# Baked into the production Vite bundle at `npm run build` time (mode=production).
# Public (non-secret) prompt-run-v1 server URL — see contracts/prompt-run-v1.md and
# src/vite-env.d.ts. No credential belongs here; the bot token lives only in the
# server's own environment. Endpoint adopted in MAI-423.
VITE_PROMPT_RUN_ENDPOINT=https://n8n.st-metro.dev/prompt-pocket/api/prompt-run
